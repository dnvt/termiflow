# Maestro Doctor Guide

`/maestro:doctor` is deprecated.

Use `/maestro:health` for workflow audits and `/maestro:health --fix`
for safe repairs.

The command is kept only as a compatibility stub so older prompts
route cleanly to the current maintenance surface.
