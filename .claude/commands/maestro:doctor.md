# /maestro:doctor — Deprecated

`maestro:doctor` has been merged into `maestro:health`.

Use `maestro:health --fix` to apply safe, non-destructive repairs.
Use `maestro:health --history` for session retrospective and
improvement backlog.

This file suppresses template regeneration of the original
`maestro:doctor` command. See DEC-005.

---

END OF COMMAND
